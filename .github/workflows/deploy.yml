name: CI/CD Deploy to DigitalOcean

on:
  push:
    branches:
      - main

jobs:
  test-build:
    name: Test Build (Frontend Verification)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Verify Code References & Syntax (Lint)
        run: |
          cd frontend
          npm install
          npm run lint

      - name: Test Frontend Build
        run: |
          cd frontend
          npm run build

  deploy:
    name: Deploy to DigitalOcean
    needs: test-build
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to DigitalOcean via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          port: 22
          timeout: 60s
          command_timeout: 30m
          script: |
            echo "Starting Clean Deployment..."
            cd ~/Serene-Villa-Hiriketiya-Serene-Villa-Pvt-Ltd
            git fetch origin main
            git reset --hard origin/main
            docker compose down --remove-orphans
            docker compose build --no-cache frontend
            docker compose build backend
            docker compose up -d --force-recreate
            docker system prune -f --volumes=false
            echo "Clean Deployment Completed Successfully!"
